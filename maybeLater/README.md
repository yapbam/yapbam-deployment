# What is this folder for?

This folder contains files that are not used yet but that I plan to use (maybe) in the future.

The idea is to use the jdeployer library that contains generic task to deploy files on a remote server.

## Why not using jdeployer directly?

The jdeployer library is not public yet (because not yet ready), so I can't use it directly.